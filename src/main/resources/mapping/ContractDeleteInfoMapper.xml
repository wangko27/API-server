<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.nuls.api.server.dao.mapper.ContractDeleteInfoMapper">
    <resultMap id="BaseResultMap" type="io.nuls.api.entity.ContractDeleteInfo">
        <result column="contract_address" property="contractAddress" jdbcType="VARCHAR"/>
        <result column="creater" property="creater" jdbcType="VARCHAR"/>
        <result column="create_tx_hash" property="createTxHash" jdbcType="VARCHAR"/>
    </resultMap>
    <insert id="insert" parameterType="io.nuls.api.entity.ContractDeleteInfo">
    insert into contract_delete_info (contract_address, creater, create_tx_hash
      )
    values (#{contractAddress,jdbcType=VARCHAR}, #{creater,jdbcType=VARCHAR}, #{createTxHash,jdbcType=VARCHAR}
      )
  </insert>
    <insert id="insertSelective" parameterType="io.nuls.api.entity.ContractDeleteInfo">
        insert into contract_delete_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="contractAddress != null">
                contract_address,
            </if>
            <if test="creater != null">
                creater,
            </if>
            <if test="createTxHash != null">
                create_tx_hash,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="contractAddress != null">
                #{contractAddress,jdbcType=VARCHAR},
            </if>
            <if test="creater != null">
                #{creater,jdbcType=VARCHAR},
            </if>
            <if test="createTxHash != null">
                #{createTxHash,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <insert id="insertByBatch" parameterType="java.util.List">
        insert into contract_delete_info (contract_address, creater, create_tx_hash)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.contractAddress,jdbcType=VARCHAR}, #{item.creater,jdbcType=VARCHAR},
            #{item.createTxHash,jdbcType=VARCHAR})
        </foreach>
    </insert>

</mapper>